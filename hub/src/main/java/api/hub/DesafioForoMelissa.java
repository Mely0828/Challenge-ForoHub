package api.hub;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

// Anotación que indica que esta es una aplicación Spring Boot
@SpringBootApplication
public class DesafioForoMelissa {

    // Método principal que arranca la aplicación Spring Boot
    public static void main(String[] args) {
        // Inicia la aplicación Spring Boot pasando la clase principal y los argumentos recibidos
        SpringApplication.run(DesafioForoMelissa.class, args);
    }
}
